type Query {
  items: [Item!]!
  node: Node
  item(name: String!): Item
}

interface Node {
   id: ID!
}

type ItemCombination {
  source: Item!
  target: Item!
}

type Item : Node {
  id: ID!,
  name: String!
  myths: Boolean!
  creates: [ItemCombination!]!
  combinations: [ItemCombination!]!
}